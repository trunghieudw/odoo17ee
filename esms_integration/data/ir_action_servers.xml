<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data noupdate="1">
        <record id="send_esms_server_action" model="ir.actions.server">
            <field name="name">E-SMS Send SMS</field>
                <field name="model_id" ref="base.model_res_partner"/>
                <field name="binding_model_id" ref="base.model_res_partner"/>
                <field name="state">code</field>
                <field name="code">
#  :param phone_numbers: List phone number - Is required
#  :param content: Message need send - Is required
#  :param route_code: The route need use send sms - Is required
if records:
    phone_numbers = [rec.mobile for rec in records if rec.phone]
    content = 'Test'
    route_code = 'send_cus_care_post'
    action = env['esms.api']._send_esms(phone_numbers, content, route_code)
                </field>
        </record>
    </data>
</odoo>